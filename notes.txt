what data structure would you use for BFS and DFS?
A graph (tree could be valid answer since tree is a type of graph)

Steps for DFS on a graph 

Start at first vertex 
put in stack 
vist neighbors in abc order 
for each neighbor: 
put vertex in stack 
visit neighbor process continues 
only put unvisited things in stack 

whwat happens when you type in www.google.com and hit enter?

browser checks the cache for dns record to find IP adress of site 
if url not in cache, dns server does dns query for site 
brower initiates tcp connection w/ server 
brower senss http request to web server 
server handles request and sends back response 
server sens http response 
brower displays html content 

difference between memory heap and call stack in javaScript 
memory in a memory heap isn't allocated and released in order like in a call stack. It's based on when a function returns. like when an asynchronousrequest finishes.

what is one problem with programming languaes that are fully single-threaded?
a single operation can only be executed at one time